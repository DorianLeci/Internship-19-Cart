export enum AppPaths {
  REGISTER = "/register",
  LOGIN = "/login",
  HOME = "/",
  PRODUCTS = "/products",
  FAVORITES = "/favorites",
  ADMIN = "/admin",
  FORBIDDEN = "/forbidden",
  NON_EXSTING = "*",
}
